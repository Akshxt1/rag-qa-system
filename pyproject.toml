[project]
name = "rag-qa-system"
version = "0.1.0"
description = "RAG-based Document Q&A System using HuggingFace + Google Gemini"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = []